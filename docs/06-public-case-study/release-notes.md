# Release notes — v0.1.0

This release publishes one complete controlled vertical:

```text
validate → decide → explain → human review → export
```

The release contains three frozen scenarios, fourteen gates, deterministic
decision precedence, bounded waivers, human-review integrity controls,
evaluator-isolated metrics, public diagrams and package artifacts.

Live-model evaluation and production integration remain outside scope.

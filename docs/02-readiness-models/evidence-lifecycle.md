# Evidence lifecycle

Evidence may be:

- current and approved;
- stale at the assessment date;
- pending or rejected by its authority;
- absent;
- explicitly passing;
- explicitly failing;
- contradictory with another valid record;
- bound to the wrong candidate or version;
- tampered.

The runtime validates identity, dates, digests, approval state, and cardinality
before applying decision precedence.

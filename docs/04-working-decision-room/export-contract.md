# Export contract

JSON, Markdown, and safe standalone HTML represent the same canonical snapshot.
Each format embeds the same SHA-256 digest. The HTML has no script, remote asset,
or network dependency.
